/**
 * /dashboard — Platform Overview
 *
 * Phase 2 stub. Full implementation with KPI cards, sync health, and
 * recent orders table is delivered in Phase 3 (feat/dashboard).
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-foreground">Dashboard</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Platform overview — metrics and sync health coming in Phase 3.
      </p>
    </div>
  )
}
