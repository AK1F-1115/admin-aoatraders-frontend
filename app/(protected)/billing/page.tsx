import BillingClient from '@/components/billing/BillingClient'

/**
 * /billing — Billing Overview
 * Client-side data fetching handled by BillingClient.
 */
export default function BillingPage() {
  return <BillingClient />
}
